import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import PrizeStakingCalculator from './prizestakingcalculator';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PrizeStakingCalculator />
  </React.StrictMode>
);
